
一、链表的基本结构
链表是由一个个节点（Node）连接而成的，每个节点都是包含数据域（Data）和指针域（Next）的基本单元。链表基本元素如下：
（1）链表节点：每个节点分为两部分，即数据域和指针域；
（2）头节点：指向链表的第一个节点；
（3）尾节点：指向链表的最后一个节点；
（4）Node：链表的最后一个节点的指针域，为空；

数据域内存储的一般是整型、浮点型等数据类型，指针域内存储的是下个节点所在的内存空间地址。