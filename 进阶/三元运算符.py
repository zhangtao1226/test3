'''
三元运算符通常在 Python 里被称为条件表达式，这些表达式基于真（True）或
假（False） 的条件判断。
表达式：condition_is_true if condition else condition_is_false
'''

# is_fat = True
# state = 'fat' if is_fat else 'not fat'
# print(state)
name = ''

print(name if name else 'error')